{
	"id": "BankAccount.json",
	"title": "Bank Account",
	"description": "A product or service offered by a bank whereby one may deposit, withdraw or transfer money and in some cases be paid interest.",
	"format": "http://fibo.sdo-fibo.appspot.com/BankAccount",
	"media": {"type": "application/json;profile=http://fibo.sdo-fibo.appspot.com/BankAccount"},
	"allOf": [{"$ref": "FinancialProduct.json"}],
	"type": "object",
	"properties": {
		"bankAccountType": {"$ref": "#/definitions/bankAccountType"},
		"minimumInflow": {"$ref": "MonetaryAmount.json#/definitions/possibleRefArray"},
		"overdraftLimit": {"$ref": "MonetaryAmount.json#/definitions/possibleRefArray"}
	},
	"definitions": {
		"array": {
			"type": "array",
			"items": {"$ref": "#"}
		},
		"possibleRef": {
			"oneOf": [
				{"$ref": "#"},
				{
					"type": "string",
					"format": "uri",
					"links": [{
						"rel": "full",
						"href": "{+$}"
					}]
				}
			]
		},
		"possibleRefArray": {
			"oneOf": [
				{
					"type": "string",
					"format": "uri",
					"links": [{
						"rel": "full",
						"href": "{+$}"
					}]
				},
				{
					"type": "array",
					"items": {"$ref": "#/definitions/possibleRef"}
				}
			]
		},
		"bankAccountType": {
			"title": "Bank Account Type",
			"description": "The type of a bank account.",
			"anyOf": [
				{"type": "string"},
				{
					"type": "string",
					"format": "uri"
				}
			]
		}
	}
}