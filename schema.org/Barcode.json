{
	"id": "Barcode.json",
	"title": "Barcode",
	"description": "An image of a visual machine-readable code such as a barcode or QR code.",
	"format": "http://fibo.sdo-fibo.appspot.com/Barcode",
	"media": {"type": "application/json;profile=http://fibo.sdo-fibo.appspot.com/Barcode"},
	"allOf": [{"$ref": "ImageObject.json"}],
	"definitions": {
		"array": {
			"type": "array",
			"items": {"$ref": "#"}
		},
		"possibleRef": {
			"oneOf": [
				{"$ref": "#"},
				{
					"type": "string",
					"format": "uri",
					"links": [{
						"rel": "full",
						"href": "{+$}"
					}]
				}
			]
		},
		"possibleRefArray": {
			"oneOf": [
				{
					"type": "string",
					"format": "uri",
					"links": [{
						"rel": "full",
						"href": "{+$}"
					}]
				},
				{
					"type": "array",
					"items": {"$ref": "#/definitions/possibleRef"}
				}
			]
		}
	}
}