{
	"id": "MusicReleaseFormatType.json",
	"title": "Music Release Format Type",
	"description": "Format of this release (the type of recording media used, ie. compact disc, digital media, LP, etc.).",
	"format": "http://fibo.sdo-fibo.appspot.com/MusicReleaseFormatType",
	"media": {"type": "application/json;profile=http://fibo.sdo-fibo.appspot.com/MusicReleaseFormatType"},
	"allOf": [{"$ref": "Enumeration.json"}],
	"definitions": {
		"array": {
			"type": "array",
			"items": {"$ref": "#"}
		},
		"possibleRef": {
			"oneOf": [
				{"$ref": "#"},
				{
					"type": "string",
					"format": "uri",
					"links": [{
						"rel": "full",
						"href": "{+$}"
					}]
				}
			]
		},
		"possibleRefArray": {
			"oneOf": [
				{
					"type": "string",
					"format": "uri",
					"links": [{
						"rel": "full",
						"href": "{+$}"
					}]
				},
				{
					"type": "array",
					"items": {"$ref": "#/definitions/possibleRef"}
				}
			]
		}
	}
}