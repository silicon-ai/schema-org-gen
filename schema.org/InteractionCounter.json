{
	"id": "InteractionCounter.json",
	"title": "Interaction Counter",
	"description": "A summary of how users have interacted with this CreativeWork. In most cases, authors will use a subtype to specify the specific type of interaction.",
	"format": "http://fibo.sdo-fibo.appspot.com/InteractionCounter",
	"media": {"type": "application/json;profile=http://fibo.sdo-fibo.appspot.com/InteractionCounter"},
	"allOf": [{"$ref": "StructuredValue.json"}],
	"type": "object",
	"properties": {
		"interactionService": {"$ref": "#/definitions/interactionService"},
		"interactionType": {"$ref": "Action.json#/definitions/possibleRef"},
		"userInteractionCount": {"$ref": "#/definitions/userInteractionCount"}
	},
	"definitions": {
		"array": {
			"type": "array",
			"items": {"$ref": "#"}
		},
		"possibleRef": {
			"oneOf": [
				{"$ref": "#"},
				{
					"type": "string",
					"format": "uri",
					"links": [{
						"rel": "full",
						"href": "{+$}"
					}]
				}
			]
		},
		"possibleRefArray": {
			"oneOf": [
				{
					"type": "string",
					"format": "uri",
					"links": [{
						"rel": "full",
						"href": "{+$}"
					}]
				},
				{
					"type": "array",
					"items": {"$ref": "#/definitions/possibleRef"}
				}
			]
		},
		"interactionService": {
			"title": "Interaction Service",
			"description": "The WebSite or SoftwareApplication where the interactions took place.",
			"anyOf": [{"$ref": "SoftwareApplication.json#/definitions/possibleRef"}, {"$ref": "WebSite.json#/definitions/possibleRef"}]
		},
		"userInteractionCount": {
			"title": "User Interaction Count",
			"description": "The number of interactions for the CreativeWork using the WebSite or SoftwareApplication.",
			"type": "integer"
		}
	}
}