{
	"id": "RentalCarReservation.json",
	"title": "Rental Car Reservation",
	"description": "A reservation for a rental car.Note: This type is for information about actual reservations, e.g. in confirmation emails or HTML pages with individual confirmations of reservations.",
	"format": "http://fibo.sdo-fibo.appspot.com/RentalCarReservation",
	"media": {"type": "application/json;profile=http://fibo.sdo-fibo.appspot.com/RentalCarReservation"},
	"allOf": [{"$ref": "Reservation.json"}],
	"type": "object",
	"properties": {
		"dropoffLocation": {"$ref": "Place.json#/definitions/possibleRefArray"},
		"dropoffTime": {
			"type": "array",
			"items": {"$ref": "#/definitions/dropoffTime"}
		},
		"pickupLocation": {"$ref": "Place.json#/definitions/possibleRef"},
		"pickupTime": {"$ref": "#/definitions/pickupTime"}
	},
	"definitions": {
		"array": {
			"type": "array",
			"items": {"$ref": "#"}
		},
		"possibleRef": {
			"oneOf": [
				{"$ref": "#"},
				{
					"type": "string",
					"format": "uri",
					"links": [{
						"rel": "full",
						"href": "{+$}"
					}]
				}
			]
		},
		"possibleRefArray": {
			"oneOf": [
				{
					"type": "string",
					"format": "uri",
					"links": [{
						"rel": "full",
						"href": "{+$}"
					}]
				},
				{
					"type": "array",
					"items": {"$ref": "#/definitions/possibleRef"}
				}
			]
		},
		"dropoffTime": {
			"title": "Dropoff Time",
			"description": "When a rental car can be dropped off.",
			"type": "string",
			"format": "date-time"
		},
		"pickupTime": {
			"title": "Pickup Time",
			"description": "When a taxi will pickup a passenger or a rental car can be picked up.",
			"type": "string",
			"format": "date-time"
		}
	}
}