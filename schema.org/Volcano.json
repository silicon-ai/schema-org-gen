{
	"id": "Volcano.json",
	"title": "Volcano",
	"description": "A volcano, like Fuji san.",
	"format": "http://fibo.sdo-fibo.appspot.com/Volcano",
	"media": {"type": "application/json;profile=http://fibo.sdo-fibo.appspot.com/Volcano"},
	"allOf": [{"$ref": "Landform.json"}],
	"definitions": {
		"array": {
			"type": "array",
			"items": {"$ref": "#"}
		},
		"possibleRef": {
			"oneOf": [
				{"$ref": "#"},
				{
					"type": "string",
					"format": "uri",
					"links": [{
						"rel": "full",
						"href": "{+$}"
					}]
				}
			]
		},
		"possibleRefArray": {
			"oneOf": [
				{
					"type": "string",
					"format": "uri",
					"links": [{
						"rel": "full",
						"href": "{+$}"
					}]
				},
				{
					"type": "array",
					"items": {"$ref": "#/definitions/possibleRef"}
				}
			]
		}
	}
}