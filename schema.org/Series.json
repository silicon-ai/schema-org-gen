{
	"id": "Series.json",
	"title": "Series",
	"description": "A Series in schema.org is a group of related items, typically but not necessarily of the same kind.",
	"format": "http://fibo.sdo-fibo.appspot.com/Series",
	"media": {"type": "application/json;profile=http://fibo.sdo-fibo.appspot.com/Series"},
	"allOf": [{"$ref": "CreativeWork.json"}],
	"definitions": {
		"array": {
			"type": "array",
			"items": {"$ref": "#"}
		},
		"possibleRef": {
			"oneOf": [
				{"$ref": "#"},
				{
					"type": "string",
					"format": "uri",
					"links": [{
						"rel": "full",
						"href": "{+$}"
					}]
				}
			]
		},
		"possibleRefArray": {
			"oneOf": [
				{
					"type": "string",
					"format": "uri",
					"links": [{
						"rel": "full",
						"href": "{+$}"
					}]
				},
				{
					"type": "array",
					"items": {"$ref": "#/definitions/possibleRef"}
				}
			]
		}
	}
}