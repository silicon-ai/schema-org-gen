{
	"id": "CompoundPriceSpecification.json",
	"title": "Compound Price Specification",
	"description": "A compound price specification is one that bundles multiple prices that all apply in combination for different dimensions of consumption. Use the name property of the attached unit price specification for indicating the dimension of a price component (e.g. \"electricity\" or \"final cleaning\").",
	"format": "http://fibo.sdo-fibo.appspot.com/CompoundPriceSpecification",
	"media": {"type": "application/json;profile=http://fibo.sdo-fibo.appspot.com/CompoundPriceSpecification"},
	"allOf": [{"$ref": "PriceSpecification.json"}],
	"type": "object",
	"properties": {"priceComponent": {"$ref": "UnitPriceSpecification.json#/definitions/possibleRefArray"}},
	"definitions": {
		"array": {
			"type": "array",
			"items": {"$ref": "#"}
		},
		"possibleRef": {
			"oneOf": [
				{"$ref": "#"},
				{
					"type": "string",
					"format": "uri",
					"links": [{
						"rel": "full",
						"href": "{+$}"
					}]
				}
			]
		},
		"possibleRefArray": {
			"oneOf": [
				{
					"type": "string",
					"format": "uri",
					"links": [{
						"rel": "full",
						"href": "{+$}"
					}]
				},
				{
					"type": "array",
					"items": {"$ref": "#/definitions/possibleRef"}
				}
			]
		}
	}
}